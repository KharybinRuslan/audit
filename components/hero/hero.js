/**
 * Логика формы «Запрос на аудит» (маска телефона, кастомный селект) перенесена в
 * /components/audit-request-form/audit-request-form.js — файл подключается из audit-modal.php
 * на всех страницах с модалкой заявки (включая главную, контакты, новости и т.д.).
 * Этот файл оставлен на случай прямых ссылок; можно удалить после проверки деплоя.
 */
(function () {
  'use strict';
})();
